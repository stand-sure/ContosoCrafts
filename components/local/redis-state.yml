apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
  namespace: default
spec:
  type: state.redis
  metadata:
    - name: redisHost
      value: redis_service:6379
    - name: redisPassword
      secretKeyRef:
        name: redis
        key: redisPassword
    # - name: redisPassword
    #   value: S0m3P@$$w0rd
auth:
  secretStore: vault
